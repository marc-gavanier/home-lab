---
# Backup disk: USB-SATA quirk, ext4 (no LUKS — ADR-010), mount.

# --- USB-SATA bridge quirk ---
# Some bridges (ASMedia ASM1153 in the 52Pi tower) corrupt writes in UAS
# mode on the Pi. usb_storage_quirks (host_vars) forces plain usb-storage.

- name: disk | Read cmdline.txt
  ansible.builtin.slurp:
    src: /boot/firmware/cmdline.txt
  register: offsite_cmdline
  when: usb_storage_quirks is defined

- name: disk | Disable UAS for the USB-SATA bridge
  ansible.builtin.lineinfile:
    path: /boot/firmware/cmdline.txt
    backrefs: true
    regexp: '^(.*?)(\s*)$'
    line: '\1 usb-storage.quirks={{ usb_storage_quirks }}\2'
  when:
    - usb_storage_quirks is defined
    - "('usb-storage.quirks=' ~ usb_storage_quirks) not in (offsite_cmdline.content | b64decode)"
  register: offsite_quirk_added

# Not a handler on purpose: handlers fire at end of play, but the quirk must
# be active BEFORE the format/mount tasks below ever touch the disk.
- name: disk | Reboot to apply the storage quirk (must precede any disk write)  # noqa: no-handler
  ansible.builtin.reboot:
    reboot_timeout: 300
  when: offsite_quirk_added is changed

# --- Backup disk (ext4, no LUKS) ---

# `check_mode: false` on both blkid probes, for the same reason as the
# rest-server version probe: Ansible skips `command` under --check, and a
# skipped command still registers `stdout` — as an EMPTY STRING, not as
# undefined (measured 2026-08-17). Nothing errors; the values simply become
# wrong, quietly.
#
# Here that produced two lies in one dry run. This probe's '' passes the
# safety check below, so the guard that refuses to overwrite an unexpected
# filesystem cannot fire in check mode. And the UUID probe's '' turns the
# mount's src into a bare `UUID=`, which no longer matches fstab, so a dry run
# announced it would REMOUNT the backup disk on the host that only exists to
# hold the last copy. Both probes only read.
# The device has to exist before its filesystem type means anything. blkid
# returns 2 both for "no filesystem here" and for "no such device", and the
# guard below reads an empty stdout as the first — so a device that moved from
# under us would have been read as a blank disk, and the format task after it
# does not ask twice.
- name: disk | The backup disk must be a block device before it is read
  ansible.builtin.stat:
    path: "{{ backup_disk_device }}"
  register: backup_disk_stat
  check_mode: false

- name: disk | Refuse to reason about a device that is not there
  ansible.builtin.assert:
    that:
      - backup_disk_stat.stat.exists
      - backup_disk_stat.stat.isblk | default(false)
    fail_msg: >-
      {{ backup_disk_device }} is not a block device on this host. Nothing below
      may run: the filesystem probe cannot tell that apart from an unformatted
      disk, and the next task formats.
    quiet: true

- name: disk | Read current filesystem type on the backup disk
  ansible.builtin.command: blkid -o value -s TYPE {{ backup_disk_device }}
  register: backup_disk_fstype
  changed_when: false
  # rc=2 is "no filesystem yet", which is a real answer. `failed_when: false`
  # accepted every OTHER rc as the same answer — blkid absent, permission
  # refused, the device gone — and each of those yields an empty stdout, which
  # the guard below has in its pass set. An instrument that could not look was
  # indistinguishable from a disk with nothing on it, one task above a format.
  failed_when: backup_disk_fstype.rc not in [0, 2]
  check_mode: false

- name: disk | Refuse to overwrite an unexpected filesystem
  ansible.builtin.fail:
    msg: >-
      {{ backup_disk_device }} carries a '{{ backup_disk_fstype.stdout }}'
      filesystem. Set offsite_disk_force_format=true (one run) to format it.
  when:
    - backup_disk_fstype.stdout not in ['', 'ext4']
    - not offsite_disk_force_format

- name: disk | Create ext4 filesystem on the backup disk
  community.general.filesystem:
    fstype: ext4
    dev: "{{ backup_disk_device }}"
    force: "{{ offsite_disk_force_format }}"

- name: disk | Read backup disk UUID
  ansible.builtin.command: blkid -o value -s UUID {{ backup_disk_device }}
  register: backup_disk_uuid
  changed_when: false
  check_mode: false

# `passno: 2` is what makes anything ever check this disk (#260). It held the
# entire offsite restic copy for 48 days with `Maximum mount count: -1`,
# `Check interval: 0` and `passno 0` — never checked since mkfs, and nothing
# that would ever check it. The superblock triggers armed in `base` are the
# other half and are useless without this one: only the boot-time fsck consults
# them.
#
# Two consequences, both accepted deliberately rather than discovered later:
#
#   COST, measured on the supervised reboot of 2026-08-29 02:41 rather than
#   estimated. The frozen boot clock (see base/tasks/filesystem-checks.yml)
#   makes every superblock look future-dated, so this IS a full check at every
#   boot rather than only when a trigger is due — confirmed in the journal:
#   `/dev/sda1: Superblock last write time (Sat Aug 29 02:40:41 2026,` … then
#   `21986/122093568 files (4.8% non-contiguous), 99978784/488366820 blocks`.
#
#   It cost **10.5 s** for the check and **+1.6 s** on the total boot, which
#   went 29.8 s -> 31.4 s: the fsck runs alongside other units rather than in
#   front of them. This paragraph first said "of the order of a minute",
#   extrapolated from the filesystem's 122 M inodes against the root's 15.5 M.
#   That was wrong by a factor of thirty-five, because the scan follows the
#   inodes actually IN USE — 21 986 of them — not the inode count.
#
#   FAILURE MODE. `nofail` is what makes that safe to do on a host with no
#   physical access: it puts the mount unit in local-fs.target's `Wants=` and
#   not its `Requires=`, so an e2fsck that refuses to preen fails the mount and
#   the boot continues, rather than dropping to an emergency shell nobody can
#   reach. That is the opposite of the root filesystem's trade-off, and it is
#   the reason this could be turned on here at all.
- name: disk | Mount backup disk
  ansible.posix.mount:
    path: "{{ backup_mount_point }}"
    src: "UUID={{ backup_disk_uuid.stdout }}"
    fstype: ext4
    opts: defaults,noatime,nofail
    passno: "2"
    dump: "0"
    state: mounted
