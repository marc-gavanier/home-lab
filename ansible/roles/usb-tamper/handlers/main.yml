---
# [critical]: reload only, never `udevadm trigger`: replaying add events with the tamper rule armed powers the host off
- name: Reload udev rules
  ansible.builtin.command:
    cmd: udevadm control --reload-rules
  changed_when: true
