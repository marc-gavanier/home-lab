---
# Input contract — validated automatically at run time (ansible-core >= 2.11).
argument_specs:
  main:
    short_description: USB tamper poweroff (udev-triggered, ADR-008)
    description: Self-contained — no input variables; everything ships in the role's files.
    options: {}
