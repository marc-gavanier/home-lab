---
argument_specs:
  main:
    short_description: Staged container startup + crash heal (ADR-007)
    description: Self-contained — no input variables; everything ships in the role's files.
    options: {}
