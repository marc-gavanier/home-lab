---
# =============================================================================
# Role: docker — Install Docker Engine and Compose
# =============================================================================
# Thin orchestrator: one file per concern, task names prefixed with the file.

- ansible.builtin.import_tasks: install.yml             # engine, compose, daemon.json, networks
- ansible.builtin.import_tasks: unlock-integration.yml  # no Docker before the LUKS mount
