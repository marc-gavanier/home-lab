---
# =============================================================================
# Role: docker — Install Docker Engine and Compose
# =============================================================================
# Thin orchestrator: one file per concern, task names prefixed with the file.

- name: Install Docker Engine, Compose and networks
  ansible.builtin.import_tasks: install.yml

- name: Gate Docker behind the homelab-unlock workflow
  ansible.builtin.import_tasks: unlock-integration.yml
