all:
  hosts:
    homelab:
      ansible_host: "{{ homelab_ip }}"
      ansible_port: "{{ ssh_port | default(22) }}"
      ansible_user: marc-gavanier
  vars:
    ansible_python_interpreter: /usr/bin/python3
