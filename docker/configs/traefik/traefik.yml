# =============================================================================
# Traefik — Static configuration
# =============================================================================

api:
  dashboard: true
  insecure: false

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
    http:
      middlewares:
        - vpn-only@file   # All services require VPN/LAN access by default
      tls:
        certResolver: letsencrypt
  ping:
    address: ":8082"      # internal only (not published) — for the container healthcheck

# Health endpoint used by the container healthcheck (`traefik healthcheck`).
# Bound to the internal ping entryPoint above — no TLS, no vpn-only middleware.
ping:
  entryPoint: ping

certificatesResolvers:
  letsencrypt:
    acme:
      email: "{{ traefik_acme_email }}"
      storage: /acme/acme.json
      httpChallenge:
        entryPoint: web

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: proxy
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: WARN

accessLog:
  filePath: /dev/stdout
  filters:
    statusCodes:
      - "400-599"
